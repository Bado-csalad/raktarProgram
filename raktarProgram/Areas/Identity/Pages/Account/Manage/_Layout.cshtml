@{
    if (ViewData.TryGetValue("ParentLayout", out var parentLayout))
    {
        Layout = (string)parentLayout;
    }
    else
    {
        Layout = "/Areas/Identity/Pages/_Layout.cshtml";
    }
}

<h2>Felhasználói beálítások</h2>

<div>
    <h4>Felhasználói beállítások megváltoztatása</h4>
    <hr />
    <div class="row">
        <div class="col-md-3">
            <partial name="_ManageNav" />
        </div>
        <div class="col-md-9">
            Fejlesztés alatt
            @*@RenderBody()*@
        </div>
    </div>
</div>

@section Scripts {
    @RenderSection("Scripts", required: false)
}
